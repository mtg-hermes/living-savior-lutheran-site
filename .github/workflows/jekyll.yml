name: Jekyll site CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '2.6.5'

    - name: Install dependencies
      run: bundle install

    - name: Build Jekyll site
      run: bundle exec jekyll build

    - name: Add noindex robots.txt for preview deploy
      if: github.event_name == 'pull_request'
      run: |
        cat <<'EOF' > ./_site/robots.txt
        User-agent: *
        Disallow: /
        EOF

    - name: Deploy preview via FTP
      # Fork PRs do not receive repository secrets, so this step must be skipped.
      if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && secrets.FTP_USERNAME != '' && secrets.FTP_PASSWORD != ''
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.livingsaviorlutheran.org
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD  }}
        local-dir: ./_site/
        server-dir: /preview.livingsaviorlutheran.org/

    - name: Deploy via FTP
      if: github.event_name == 'push' && github.ref == 'refs/heads/master'
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.livingsaviorlutheran.org
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD  }}
        local-dir: ./_site/
        server-dir: /public_html/
